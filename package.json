{
  "name": "react-node-concurrent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "install-all": "npm run install-current && npm run install-frontend && npm run install-backend",
    "install-current": "npm install",
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && npm install"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "marked": "^15.0.3"
  }
}
